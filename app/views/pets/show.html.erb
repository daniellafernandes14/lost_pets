<h1><%= @pet.name %> </h1>
<h2><%= "Breed: #{@pet.breed}" %> </h2>
<h2><%= "Where: #{@pet.address}" %> </h2>
<h2><%= "Found on: #{@pet.found_on}" %> </h2>

<%= link_to 'Back to list', pets_path %>

<%= link_to 'Edit Pet', edit_pet_path %>

<%= link_to 'Delete Pet', pet_path(@pet), method: :delete, data: {confirm: 'Are you sure?'} %>
